import styled from 'styled-components'
import { cores } from '../../styles'

export const Carregando = styled.p`
  color: ${cores.rosa};
  text-align: center;
  padding: 80px 0;
  font-size: 16px;
  font-weight: bold;
`
